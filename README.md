# pod

## Usage

```bash
$ pod node -e "console.log('hello world')"
$ pod npx create-react-app --template typescript
$ pod -p 3000 npm start
```

### Pro tip
```bash
# ~/.bashrc
alias node='pod node'
alias npm='pod npm'
...
```